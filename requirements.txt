# Dépendances MkDocs pour Formation Systeme.io
# Installation : pip install -r requirements.txt

# Core MkDocs avec thème Material
mkdocs>=1.6.0
mkdocs-material>=9.7.0

# Plugins essentiels
mkdocs-git-revision-date-localized-plugin>=1.5.0
mkdocs-print-site-plugin>=2.8

# Dépendances pour les plugins
GitPython>=3.1.44

# Note: mkdocs-minify-plugin désactivé (problème d'installation avec Python 3.11+)
# Peut être réactivé ultérieurement avec un environnement virtuel

# Optionnel : Export PDF avancé (décommenter si nécessaire)
# mkdocs-with-pdf>=0.9.0
# weasyprint>=60.0

# Optionnel : Scripts d'automatisation (décommenter si utilisés)
# Pillow>=10.0.0              # optimize_images.py
# PyYAML>=6.0                 # validate_frontmatter.py (déjà requis par MkDocs)
# pyspellchecker>=0.7.0       # check_spelling.py
# requests>=2.31.0            # check_links.py (vérification liens externes)
