name: Publier la documentation

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Nécessaire pour git-revision-date (historique complet)

      - name: Configurer Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.x
          cache: 'pip'

      - name: Installer les dépendances
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Valider la documentation
        run: |
          echo "🔍 Validation de la structure et du build..."
          mkdocs build --strict
          echo "✅ Build validé avec succès"

      - name: Vérifier les liens
        run: |
          echo "🔗 Vérification des liens internes..."
          python scripts/check_links.py
          echo "✅ Liens vérifiés avec succès"

      - name: Déployer sur GitHub Pages
        run: mkdocs gh-deploy --force
